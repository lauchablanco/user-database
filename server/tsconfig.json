{
  "compilerOptions": {
    "target": "ESNext",                // Usamos ESNext para soportar la última versión de ECMAScript
    "module": "ESNext",                // Cambiado a ESNext para asegurar que TypeScript emita ESM
    "moduleResolution": "Node",        // Usamos resolución de módulos estándar de Node
    "rootDir": "./src",                // Directorio raíz donde están los archivos .ts
    "outDir": "./dist",                // Directorio donde se generarán los archivos compilados
    "esModuleInterop": true,           // Asegura que los módulos de CommonJS puedan ser importados en ESM
    "forceConsistentCasingInFileNames": true, // Para asegurar que las importaciones sean consistentes
    "strict": true,                    // Para habilitar todas las comprobaciones estrictas
    "skipLibCheck": true               // Para evitar la comprobación de tipos en los archivos .d.ts
  },
  "include": ["src/**/*.ts"],           // Asegura que se incluyan todos los archivos .ts en src
  "exclude": ["node_modules"]           // Excluye la carpeta node_modules de la compilación
}
